import React from "react";

function Try2(props) {
  return (
    <div>
      <h1>Name: {props.name}</h1>
      <p>Age: {props.age}</p>
      <p>Residence: {props.home}</p>
    </div>
  );
}

export default Try2;
